% ============================================================
%  modern_cv.tex  –  drop-in replacement for simple_cv.tex
% ============================================================
\documentclass[10pt, a4paper]{article}

% ── Page geometry ────────────────────────────────────────────
\usepackage[
  top=0.60in, bottom=0.55in,
  left=0.70in, right=0.70in
]{geometry}

% ── Core packages ────────────────────────────────────────────
\usepackage[T1]{fontenc}
\usepackage{lmodern}           % crisp vector fonts
\usepackage{microtype}         % optical margin alignment
\usepackage{xcolor}
\usepackage{hyperref}
\usepackage{tabularx}
\usepackage{array}
\usepackage{enumitem}          % tight bullet lists
\usepackage{parskip}
\usepackage{titlesec}
\usepackage{fancyhdr}
\usepackage{graphicx}
\usepackage{multirow}

% ── Brand colour palette ─────────────────────────────────────
\definecolor{accent}{HTML}{1A56DB}   % strong blue
\definecolor{rule}{HTML}{CBD5E1}     % light slate divider
\definecolor{muted}{HTML}{64748B}    % slate-500 for meta text
\definecolor{dark}{HTML}{0F172A}     % near-black for headings

% ── Hyperlink style ──────────────────────────────────────────
\hypersetup{
  colorlinks = true,
  urlcolor   = accent,
  linkcolor  = accent,
  pdfborder  = {0 0 0}
}

% ── Section heading style ────────────────────────────────────
%   Solid accent left bar + ALL-CAPS small label + full-width rule
\titleformat{\section}
  {\normalfont\bfseries\color{dark}}   % format
  {}                                   % label
  {0pt}                                % sep
  {%
    \color{accent}\rule[-2pt]{3pt}{11pt}\hspace{6pt}%
    \MakeUppercase%
  }
  [\vspace{-6pt}\textcolor{rule}{\hrule height 0.6pt}\vspace{2pt}]

\titlespacing{\section}{0pt}{10pt}{4pt}

% ── Bullet list style ────────────────────────────────────────
\setlist[itemize]{
  leftmargin = 1.1em,
  itemsep    = 2pt,
  parsep     = 0pt,
  topsep     = 2pt,
  label      = {\color{accent}\textbullet}
}

% ── Footer ───────────────────────────────────────────────────
\pagestyle{fancy}
\fancyhf{}
\renewcommand{\headrulewidth}{0pt}
\fancyfoot[C]{%
  \vspace{4pt}
  \textcolor{muted}{\footnotesize Generated by Learning Coach AI Platform}%
}

% ============================================================
\begin{document}
\setlength{\parskip}{4pt}

% ── ❶  Header block ──────────────────────────────────────────
\begin{center}
  {\fontsize{26}{28}\selectfont\bfseries\color{dark}{{NAME}}}\\[4pt]
  \textcolor{muted}{\small
    \href{mailto:{{EMAIL}}}{{{EMAIL}}}
    \quad\textbullet\quad {{MOBILE}}
    \quad\textbullet\quad {{LOCATION}}
  }
\end{center}

\vspace{4pt}

% ── ❷  Professional Summary ──────────────────────────────────
\section{Professional Summary}
\textcolor{dark}{{{SUMMARY}}}

% ── ❸  Skills ────────────────────────────────────────────────
\section{Skills}
{{SKILLS}}

% ── ❹  Experience ────────────────────────────────────────────
\section{Experience}
{{EXPERIENCES}}

% ── ❺  Projects ──────────────────────────────────────────────
\section{Projects}
{{PROJECTS}}

% ── ❻  Education ─────────────────────────────────────────────
\section{Education}
{{EDUCATIONS}}

% ── ❼  Certifications ────────────────────────────────────────
\section{Certifications}
{{CERTIFICATES}}

\end{document}
